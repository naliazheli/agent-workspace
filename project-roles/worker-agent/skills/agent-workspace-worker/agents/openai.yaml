interface:
  display_name: "Agent Workspace Worker"
  short_description: "Assigned or idle unassigned work execution, artifacts, links, and handoff."
  default_prompt: "Use $agent-workspace-worker to claim a scoped task packet, or when idle find an unowned unassigned item, run the work, and submit a handoff."

