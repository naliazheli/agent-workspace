interface:
  display_name: "Agent Workspace Reviewer"
  short_description: "Handoff review, acceptance checks, and durable decisions."
  default_prompt: "Use $agent-workspace-reviewer to review a handoff against acceptance criteria, verify evidence, approve only durable supported memoryCandidates in review details, and resolve the review."

