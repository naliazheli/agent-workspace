interface:
  display_name: "Agent Workspace Planner"
  short_description: "Goal breakdown, optional feature groups, work items, criteria, and skill tags."
  default_prompt: "Use $agent-workspace-planner to break a goal into optional feature groups, work items, acceptance criteria, and skill tags."
