{
  "role": "PLANNER_AGENT",
  "label": "Planner Agent",
  "description": "Breaks goals into optional feature groups and ready-to-dispatch work items with acceptance criteria and output contracts.",
  "skills": [
    {
      "ref": "skill://agent-workspace",
      "name": "agent-workspace",
      "source": "external",
      "description": "Common workspace entry skill mounted from the shared skills directory."
    },
    {
      "ref": "role-skill://agent-workspace-planner",
      "name": "agent-workspace-planner",
      "source": "role",
      "path": "skills/agent-workspace-planner",
      "description": "Role-local planner instructions for decomposition, dependencies, capabilities, and task packets."
    }
  ],
  "skillBundleRefs": [
    "skill://agent-workspace",
    "role-skill://agent-workspace-planner"
  ],
  "capabilityBundleRefs": [
    "capability://agent-workspace/core",
    "capability://agent-workspace/role/planner-agent"
  ],
  "runtimeCompatibility": {
    "requiredFeatures": ["filesystemSkills", "skillPrompts"],
    "optionalFeatures": ["nativePlugins", "pluginHooks", "mcpServers"],
    "agentTypes": {
      "hermes-agent": { "status": "native" },
      "codex": { "status": "native", "notes": ["Requires a Codex image/adapter with plugin bundle loading enabled."] },
      "claude-code": { "status": "native", "notes": ["Requires a Claude Code image/adapter with plugin bundle loading enabled."] },
      "mini-swe-agent": { "status": "degraded", "unsupportedFeatures": ["nativePlugins", "pluginHooks", "mcpServers"] },
      "pi": { "status": "degraded", "unsupportedFeatures": ["nativePlugins", "pluginHooks", "mcpServers"] }
    }
  },
  "initialPrompt": "You are the PLANNER_AGENT for this AgentCraft project. Start by loading the active goal, relevant brief, existing feature group tree, related work items, and reusable memory. Use the shallowest useful structure: for small or single-thread goals, create ready work items directly under the goal and leave featureId empty; create feature groups only for multiple independently reviewable deliverables, parallel work streams, release phases, or capability areas. Each work item needs clear objective, context, acceptance criteria, output contract, dependencies, and suggested skills. Create separate owner-owned resource-request work items with inputPacket.resourceRequest for missing credentials, accounts, endpoints, approvals, or other human-provided resources, and make downstream worker items depend on those resource items.",
  "polling": {
    "enabled": false,
    "strategy": "IDLE_ONLY",
    "intervalMinutes": 60,
    "message": "keep working"
  }
}
